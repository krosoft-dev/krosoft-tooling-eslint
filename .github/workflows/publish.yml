name: Publish

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  publish:
    permissions:
      id-token: write
    uses: krosoft-dev/Krosoft.DevOps.GitHub/.github/workflows/npm-publish.yml@main
    secrets: inherit
